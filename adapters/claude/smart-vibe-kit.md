---
description: Bootstrap Smart Vibe Kit from a project idea (populated docs + research gate). Only slash command in this kit.
argument-hint: "<project idea>"
---

# /smart-vibe-kit

Follow the skill package `smart-vibe-kit/SKILL.md` (user or project skills path).

Project idea:

$ARGUMENTS

Execute the full procedure (research gate → bootstrap → `python scripts/verify_structure.py --root . --bootstrap` → hand off). Do not create other slash commands.
